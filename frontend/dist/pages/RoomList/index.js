export function renderRoomListPage() {
    return `
    <section class="card">
      <p class="eyebrow">Room Page Placeholder</p>
      <h2>Waiting for room query APIs</h2>
      <p class="muted">
        The backend currently exposes room creation only. Room list, room status filters, and room detail endpoints are not available yet, so this page stays as a ready-made integration slot.
      </p>
    </section>
  `;
}
//# sourceMappingURL=index.js.map